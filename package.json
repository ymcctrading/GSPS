{
  "name": "gsps-engine",
  "version": "0.1.0",
  "private": true,
  "description": "GSPS engine, backend, and drop-in UI components (scanner waterfall, market-close cron, multi-asset automation, tier gating).",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/react": "^18.3.31",
    "@types/ws": "^8.18.1",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
